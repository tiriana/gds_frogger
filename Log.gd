extends "res://scenes/Terrian.gd"

func _ready():
	pass
	
#func init(middle_pieces):
#	$TileMap.set_cell(0, 0, "First")
#	$TileMap.set_cell(middle_pieces + 1, 0, "Last")
#	for i in range(middle_pieces):
#		$TileMap.set_cell(1 + i, 0, "Middle")
	

#func _process(delta):
#	# Called every frame. Delta is time since last frame.
#	# Update game logic here.
#	pass